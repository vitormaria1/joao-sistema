name: Legacy Deploy Disabled

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment handled on VPS
        run: |
          echo "SSH deploy disabled. VPS pulls main on its own."
